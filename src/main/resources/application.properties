spring.application.name=grc-score-service

# ===== DATABASE CONFIG (AWS RDS) =====
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://scrapdms.cbs2gecycm7e.ap-south-1.rds.amazonaws.com:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:appuser}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:oUnvpNiOQPvPH11C0C2g}

# ===== JPA / HIBERNATE =====
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.sql.init.mode=never

# ===== CONNECTION POOL (Hikari) =====
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000

# ===== EXTERNAL GST API (REMOVED) =====
# Kashidigital API removed — GRC scores are now calculated from manually entered GST details.

server.port=${PORT:8080}
 
# ===== MAIL CONFIG =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=scrapbiddate@gmail.com
spring.mail.password=eaivigmrryjhajio
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
